schemaVersion: 1

source:
  # NOTE: repo path is resolved relative to this config file directory.
  # Since this file lives at `.governance/config.yaml`, `..` points at the repo root.
  repo: ".."
  ref: "HEAD"
  profile: "mobile-clean-ios"

paths:
  docsRoot: "."

sync:
  managedBlockPrefix: "GOV"
  localAddendaHeading: "Local Addenda (project-owned)"

