schemaVersion: 1

source:
  # NOTE: repo path is resolved relative to this config file directory.
  # This config lives at `tools/gov/.governance/config.yaml`, so `../../..` is the repo root.
  repo: "../../.."
  ref: "HEAD"
  profile: "backend-go-hex"

paths:
  # Emit docs into the embedded repo root (`tools/gov/`).
  docsRoot: "."

sync:
  managedBlockPrefix: "GOV"
  localAddendaHeading: "Local Addenda (project-owned)"

